if ():#variable (<,>,==,!=,<=,>=) (variable o numero/palabra):
    #Intruccion (Verdadera)
else:
    #Intruccion falsa / si no pasa eso
    
#puede incluirse un if dentro de un else para sumar mas posibilidades de condiciones ej:
if ("Prueba"):
    "Instruccion"
else:
    if ("identado en else para agregar if y seguir creando posibilidades") #Se puede terminar en if si queremos


#(Aclaracion: and significa Y, quiere decir que se debe cumpliar ambas instrcciones para ejecutar if)
#(Aclaracion: or significa O, quiere decir que se debe cumplir una sola instruccion para ejecutar if)
#(Aclaracion: not significa NO, quiere decir que si es invierte los operadores booleanos, if pasa a ser false y else pasa hacer true)
#(not*lo verdadero es falso y lo falso verdadero)

if Variable,operador,numero,palabra and,or,not Variable,operador,numero,palabra:
    Instruccion
else:
    instruccion




#Validar datos: La validación de datos
#Es el proceso de asegurar que un programa funcione en datos limpios, correctos y útiles.
#La validación de datos compruebe que los datos están aptos para el propósito deseado,​ válidos, razonables y seguros antes de su procesamiento.
#Es decir que nos aseguremos de que si nos piden una edad validar que el ususario no escriba en negativo, y esas tipo de cosas
#Ejemplo:


